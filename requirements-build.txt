-r requirements.txt
pyinstaller==6.22.3

# Release builds additionally install the transitive, hash-locked closure in
# requirements-release.lock. That closure pins pip-audit and audits the exact
# venv that invokes PyInstaller.
